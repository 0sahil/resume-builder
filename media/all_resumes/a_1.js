$( document ).ready(function() {
    console.log( '{{ c_user }}' ) 
    console.log('hi');
    // $('#name').val({{c_user.f_name}} + " " + {{c_user.m_name}} + " " + {{c_user.l_name}});
    // $('#job_role').val({{c_user.job_role}});
    // $('#email_add').val({{c_user.email_add}});
    // $('#mobile_no').val({{c_user.mobile_no}});
    // console.log({{users.vehicle}});
});

$( window ).on( "load", function() {
    // console.log( "window loaded" );
});